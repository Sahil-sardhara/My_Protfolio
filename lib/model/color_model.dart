import 'package:flutter/material.dart';

class ColorModel {
  String svgpath;
  Gradient  gradient;
  Color color;

  ColorModel({
    required this.svgpath,
    required this.gradient,
    required this.color,
  });
}
