import 'package:device_frame/device_frame.dart';
import 'package:flutter/widgets.dart';

class DeviceModel {
  DeviceInfo device;
  IconData data;

  DeviceModel({required this.device, required this.data});
}
